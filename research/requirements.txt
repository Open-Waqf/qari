# --- Core ML ---
# This installs the latest stable TensorFlow (currently 2.16.x)
# It automatically includes Keras 3.
setuptools<81
tensorflow[and-cuda]

# --- Audio Processing ---
librosa==0.10.1
pydub
audiomentations
scikit-learn==1.3.2

# --- Utilities ---
numpy<2.0.0
# We pin numpy < 2.0 because TF 2.16 still has some warnings with NumPy 2.0
# ensuring 1.26.x keeps everything stable.

# --- Conversion Tools (LOCAL ONLY) ---
# These will only install on your Windows machine
tensorflowjs; sys_platform == "win32"
tf-keras; sys_platform == "win32"

playwright>=1.41.0
pytest-playwright>=0.4.0